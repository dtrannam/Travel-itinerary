selectedDays = document.querySelector('#days')


selectedDays.addEventListener('input', () => {
    start = 0
    // remove all elemenets of class x 

    // recreate all elements of class x and provide ID based on start
    
})